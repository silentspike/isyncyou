#639 S-AG.14 — on-device live official-OAuth handoff (redacted: no OAuth URL/state/code,
token, refresh, account id, IP, or device serial). Device: Pixel 8 Pro (husky), USB,
device-lock ag-639. Default DEBUG product APK com.silentspike.isyncyou.debug.

PROVENANCE: this live evidence was captured against implementation commit
d5bee49d8daaa653bad3cb6941e7bf61d30d304c. It is retained as historical flow evidence; it does
not attest implementation commit a84b286569be88a672f9c6a67eb7ad55b6360a61 or its APK. See
../current-tree-recheck.txt for the current blocked device status.

DEFAULT PRODUCT APK
-------------------
- sha256 = bab36ef09b50027b9a0374387f9faa74dcae27a58ca259e9c3d3ddbb6b28defb
  (== the release-exclusion boundary build; recorded in host-gates.txt).
- Diagnostic-hook ABSENT: 0 hits for {codex-debug, agent-network-device-test-hooks,
  codex_refresh_for_device_test, network_snapshot_test_hook, take_codex_refresh_for_device_test}
  in the APK's native libraries.

LIVE HANDOFF (user performed the real official login; NO raw-token seeding)
---------------------------------------------------------------------------
1. First launch: the Assistant wizard rendered on-device and showed the RECONNECT short flow
   ("Reconnect your AI account" + "Reconnect ChatGPT") for a pre-existing credential — proving
   #639 fail-closed: a credential without a matching ProductActivationV1 is treated as
   reconnect_required, never silently trusted. [01-wizard-reconnect-720.png]
2. ChatGPT/Codex: tapped Reconnect ChatGPT → the device browser opened the OFFICIAL OpenAI login
   (auth.openai.com/choose-an-account); after the real login the loopback callback completed,
   the credential was encrypted + activated, and the wizard was replaced by the chat surface
   showing "Connected · ChatGPT · GPT-5.5". [02-codex-connected-720.png]
3. Claude: model switcher → "Connect Claude…" → Allow Claude consent → the device browser opened
   the OFFICIAL Claude authorize page (claude.ai/oauth/authorize); after the real authorization the
   shown code was pasted into the app's type=password field (strict-JSON /oauth/complete,
   attempt-state-bound), completing to "Connected · Claude · Sonnet 5". [03-claude-connected-720.png]

RESULT
------
Both product providers reached host-verified readiness (provider_ready true, which requires a
valid Active V2 bundle + a matching ProductActivationV1 + a passing static harness attestation)
via a REAL official OAuth handoff on-device. Screens are the mobile (~720px-equivalent) evidence.
Screenshots: docs/evidence/artifacts/issue-639/device/*.png (secret-free; keyboard/clipboard
chips were dismissed/cleared before capture).

ON-DEVICE INSTRUMENTATION (device-lock ag-639, Pixel 8 Pro)
-----------------------------------------------------------
- ./gradlew :app:connectedDebugAndroidTest (AgentCredentialKeyStoreInstrumentedTest) -> BUILD
  SUCCESSFUL, 3 tests on Pixel 8 Pro (the encrypted agent credential Keystore path, relevant to #639).
- ./gradlew :app:connectedDebugAndroidTest (NetworkCriticalGuardInstrumentedTest) -> BUILD SUCCESSFUL,
  1 test on Pixel 8 Pro (the dataSync foreground guard the agent flows use).
The #639 default APK therefore passes on-device instrumentation for these representative,
non-interactive classes. #639 adds no new instrumentation classes.
