build/
.gradle/
local.properties
*.apk
*.keystore
*.jks
*.dex
# Rust JNI libs are produced before Gradle by the remote-native build boundary.
app/src/main/jniLibs/
# credentials the user supplies (never committed): release signing + FCM
google-services.json
*-signing.env
signing.properties
