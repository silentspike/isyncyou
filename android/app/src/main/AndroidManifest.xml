<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- FCM notifications (#575): Android 13+ requires a runtime grant. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Network guard during OAuth (#640): a short-lived foreground service keeps the app
         process out of the APP_BACKGROUND network restriction while the system browser is in
         front, so the loopback token exchange survives sign-in. Android 14 (targetSdk 34)
         requires the typed FOREGROUND_SERVICE_DATA_SYNC permission alongside FOREGROUND_SERVICE. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>

    <!-- The network diagnostic opens only these trusted system Settings actions. -->
    <queries>
        <intent><action android:name="android.settings.APPLICATION_DETAILS_SETTINGS"/></intent>
        <intent><action android:name="android.settings.panel.action.INTERNET_CONNECTIVITY"/></intent>
        <intent><action android:name="android.settings.IGNORE_BACKGROUND_DATA_RESTRICTIONS_SETTINGS"/></intent>
        <intent><action android:name="android.settings.IGNORE_BATTERY_OPTIMIZATION_SETTINGS"/></intent>
    </queries>

    <application
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- FCM: when the app is backgrounded the system posts the notification, so
             point it at our brand icon + accent colour instead of the default (#576). -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_stat_isyncyou"/>
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/brand_indigo"/>

        <!-- FCM push receiver (#575) — wakes the app to post a notification. -->
        <service
            android:name=".IsyncMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>

        <!-- #640: bounded interactive OAuth/refresh/turn guard. It is not the #626
             WorkManager foreground service and does not imply universal network reachability. -->
        <service
            android:name=".NetworkCriticalGuardService"
            android:exported="false"
            android:foregroundServiceType="dataSync"/>

        <!-- WorkManager's long-running Worker delegates to this service. Android 14+
             rejects the foreground transition unless both the manifest and the
             ForegroundInfo runtime value declare the dataSync type. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge"/>

        <!-- Storage Access Framework: expose OneDrive in the system Documents UI (S-OM.12, #658).
             android:permission="MANAGE_DOCUMENTS" is required OF the caller — a signature|privileged
             permission only the system DocumentsUI holds — so only the system can bind this provider.
             It is deliberately NOT a <uses-permission> (a normal app cannot hold it). -->
        <provider
            android:name=".OneDriveDocumentsProvider"
            android:authorities="${applicationId}.documents"
            android:exported="true"
            android:grantUriPermissions="true"
            android:permission="android.permission.MANAGE_DOCUMENTS">
            <intent-filter>
                <action android:name="android.content.action.DOCUMENTS_PROVIDER"/>
            </intent-filter>
        </provider>
    </application>
</manifest>
