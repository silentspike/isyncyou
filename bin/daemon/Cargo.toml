[package]
name = "isyncyou-daemon"
description = "iSyncYou engine daemon (graph/sync/store)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[[bin]]
name = "isyncyoud"
path = "src/main.rs"

[dependencies]
