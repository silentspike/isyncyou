//! `isyncyou-statusbar` — native mini status-bar app.
//!
//! Phase 0 skeleton. The renderer (tiny-skia + cosmic-text, headless-verifiable)
//! is proven in the standalone spike and will be ported here.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert_eq!(2 + 2, 4);
    }
}
