# Web UI (full control)

Static assets for the browser-based full-control UI, served by the daemon's local API
(Unix socket / TLS). Covers restore, mail/calendar/contacts/ToDo/OneNote viewers,
full-text search, job list and settings. Opened in the user's own browser — no embedded
browser engine. Placeholder in Phase 0.
