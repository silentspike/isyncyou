//! `isyncyou-pbs` — Trigger Proxmox Backup Server backup/restore (user never touches Proxmox directly).
//!
//! Phase 0 skeleton: structure only, no implementation yet.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert_eq!(2 + 2, 4);
    }
}
