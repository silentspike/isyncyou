//! `isyncyou-pathmap` — Cloud<->local namespace mapping with a persistent, reversible mapping table.
//!
//! Phase 0 skeleton: structure only, no implementation yet.

#[cfg(test)]
mod tests {
    #[test]
    fn smoke() {
        assert_eq!(2 + 2, 4);
    }
}
