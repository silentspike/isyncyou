//! `isyncyou-acceptance` — houses the v0.1 acceptance harness.
//!
//! The acceptance criteria A1–A10 (plan §19) are verified as integration tests
//! in `tests/mvp.rs`, exercising the real engine crates. Full end-to-end
//! acceptance (Flatpak install → login → the chaos matrix of §20) additionally
//! requires the assembled daemon + a display and is tracked separately.
